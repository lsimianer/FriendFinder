#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../gnodemon/bin/gnodemon" "$@"
  ret=$?
else 
  node  "$basedir/../gnodemon/bin/gnodemon" "$@"
  ret=$?
fi
exit $ret
